name: Ejercicio2

on:
  workflow_dispatch:
    inputs:
      image_name:
        description: 'hagman-front'
        required: true

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - name: si
        run: |
          npm ci 
          npm run build --if-present
          echo "${{ secrets.TOKEN_PASSWORD }}" | docker login https://ghcr.io -u hugonico7 --password-stdin
