name: Ejercicio2

on:
  workflow_dispatch:
    inputs:
      image_name:
        description: 'hagman-front'
        required: true

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - shell: bash
        env:
          TOKEN_PASSWORD: ${{ secrets.TOKEN_PASSWORD }}
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - name: Build Docker Image
        run: |
          npm ci 
          npm run build --if-present
          echo ${{ secrets.TOKEN_PASSWORD }}
          docker login ghcr.io -u hugonico7 -p ${{ secrets.TOKEN_PASSWORD }}
